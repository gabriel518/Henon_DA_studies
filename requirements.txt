numpy
scipy
matplotlib
numba
xsuite        # meta-package: bundles xtrack, xpart, xobjects (+ the
              # xsuite-prebuild command used in install_miniforge.sh)
nafflib
ipykernel
# optional, for GPU tracking (pick the build matching your CUDA version --
# cupy-cuda11x works fine even with a newer driver, it bundles its own
# CUDA runtime; see install_miniforge.sh):
# cupy-cuda11x
